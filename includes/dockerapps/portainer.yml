
#Portainer
portainer:
  restart: always
  image: portainer/portainer
  container_name: portainer
  ports:
    - %PORT%:9000
  volumes:
    - /var/run/docker.sock:/var/run/docker.sock
  privileged: true
