
#UI For Docker
ui-for-docker:
  restart: always
  image: uifd/ui-for-docker
  container_name: ui-for-docker
  ports:
    - %PORT%:9000
  volumes:
    - /var/run/docker.sock:/var/run/docker.sock
  privileged: true
